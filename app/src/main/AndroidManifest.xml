<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.isetkl.application_produiteshop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".formulaire_nv_inscri"></activity>
        <activity android:name=".formulaire_inscri" />
        <activity android:name=".confirmation" />
        <activity android:name=".huile_noble" />
        <activity android:name=".typecoiffure" />
        <activity android:name=".huilecorps" />
        <activity android:name=".huilevisage" />
        <activity android:name=".typehuile" />
        <activity android:name=".categorie_deProduit" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>